import PlanSummary from "../../components/PlanSummary";

export const metadata = {
  title: "Your plan — Moving In · Space Plan",
  description: "Everything you decided, in one place: what is coming, what is being sold, and what you still have to find.",
};

export default function PlanPage() {
  return <PlanSummary />;
}
